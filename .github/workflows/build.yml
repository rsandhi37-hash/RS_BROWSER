name: Build RS Browser
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # Updated to v4
      - name: set up JDK 11
        uses: actions/setup-java@v4 # Updated to v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: gradle
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build with Gradle
        run: ./gradlew assembleDebug
      - name: Rename and Move APK
        run: |
          mkdir -p build-output
          cp app/build/outputs/apk/debug/*.apk build-output/RS-Browser-v1.apk
      - name: Upload APK
        uses: actions/upload-artifact@v4 # Updated to v4 (Yeh asali fix hai!)
        with:
          name: RS-Browser-Package
          path: build-output/RS-Browser-v1.apk

